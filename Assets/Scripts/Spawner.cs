using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Spawner : MonoBehaviour
{
    //[SerializeField] private Transform spawnLocation;
    //public GameObject enemy;

    //private bool isEnemyOnScreenisEnemyOnScreen = false;
    

    //// Start is called before the first frame update
    //void Start()
    //{
    //    if (gameObject.name == "Enemy Spawner Column 1")
    //        enemy.transform.position = gameObject.transform.position;
    //    else if (gameObject.name == "Enemy Spawner Column 2")
    //        enemy.transform.position = gameObject.transform.position;
    //    else if (gameObject.name == "Enemy Spawner Column 3")
    //        enemy.transform.position = gameObject.transform.position;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    if(!isEnemyOnScreenisEnemyOnScreen)
    //    {
    //        Instantiate(enemy);
    //        isEnemyOnScreenisEnemyOnScreen = true;
    //    }
    //}
}
